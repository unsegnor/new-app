Add property based testing
Make a UI to create tests (read and write on a local file)
Deploy docker container as a lambda function (create a lambda folder with Dockerfile and index.js)
Write reusable test cases and reuse for component and unit testing
Use docker-compose to wait for healthy status instead of waiting a fixed time for the container to be healthy
Test deploying the application to kubernetes
Add component tests to PR validation
Use node version from nvmrc file
Ensure we use the same commands to build the container for test and manually to debug
Build the image in pre-test or post-install
